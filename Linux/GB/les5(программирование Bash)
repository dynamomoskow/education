echo {1..12}
MyVar=Goods
echo $MyVar
MyGoods="Goods the bets"
echo $MyGoods

МАССИВ
MyVar[1]=ab
MyVar[2]=cd
MyVar[4]=fe
echo ${MyVar[2]}
echo ${#MyVar} - длина массива
echo ${#MyVar[0]}- сколько символов
for i in {1..12}
do
echo $i
done

АРГУМЕНТЫ
$# - число аргументов
$0 -имя программы
$1 - первый аргумент
$2-второй аргумент
....
$9 - девятый аргумент
${10} - десятый аргумент
${11}- и так далее
$*- все аргументы

cmdfirst && cmdsecond -логическое И
cmdfirst || cmdsecond - логическое ИЛИ

в баше True=0 а False>=1

Взаимодействие процессов
cmdfirst|cmdsecond - конвейер
cmdfirst; cmdsecond -последовательный запуск

Виды команд:
- Алиасы
-Ключевые слова интерпертаторов
-Функции
-Внутренние команды(for/if/case)
-Внешние команды(cat, echo, find..)
type -проверить тип команды
