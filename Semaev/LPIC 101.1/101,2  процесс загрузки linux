BIOS/UEFI ---> MBR/GPT--->LILO/GRUB--->LInux Kernel---->init
dmesg - события ядра
cat /var/log/dmesg информация только о загрузке

Система инициализации ОС
init в стиле SysV - родительский процесс инифиализации системы на одном из заданных уровней запуска

init в стиле systemd - родительский процесс инициализации системы в ускоренном режиме , засчет паралленльного запуска задач

init в стиле Upstart - родительский процесс инициализации системы на основе отслеживания событий
